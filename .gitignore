node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
CLAUDE_CODE_HANDOFF.md
Livrables/

# NOTE: server-dist/ is deliberately NOT ignored. It is build output, but it is also the file
# the Vercel function imports, and committing it means the bundle is present in the uploaded
# source rather than depending on build ordering for functions.includeFiles to find it.
# Rebuild it with `npm run build:server` whenever backend/, services/ or database/ change.
